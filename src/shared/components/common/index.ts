/**
 * Common Components
 * 
 * This directory contains components that are used across multiple features
 * but are more complex than basic UI components. These include things like
 * search bars, social links, and other shared functionality components.
 */

// To be populated with component exports after component files are moved
export {}; 